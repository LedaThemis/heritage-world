# App configuration
VITE_APP_TITLE="Heritage World"
VITE_SERVER_URL="ws://localhost:2567"
# AI guide API base (optional). Defaults to VITE_SERVER_URL with ws->http.
# VITE_GUIDE_API_URL="http://localhost:2567"
# Aoi cloned-voice (XTTS-v2) service (optional). Defaults to http://localhost:5050.
# Run it from ../heritage-tts. If unreachable, Aoi falls back to the browser voice.
# VITE_TTS_API_URL="http://localhost:5050"
